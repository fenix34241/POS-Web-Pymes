# POS-Web-Pymes
🚀 Cómo poner en marcha el sistema
Para ejecutar este POS en tu computadora usando Docker, sigue estos pasos:

Abre una terminal dentro de la carpeta donde descargaste el proyecto.

**Ejecuta el siguiente comando:**

```bash
docker compose up -d
```
El flag -d permite que el sistema corra en segundo plano, así que puedes cerrar la terminal y el POS seguirá funcionando.

Accede al sistema: Una vez que termine de cargar, abre tu navegador y entra a http://localhost:5173 (o el puerto que hayas configurado).
